.popover-modal#favourites-popover
  .col-xs-12.col-sm-12.col-md-12.col-lg-12.content
    .title.margin-bottom-25
      %h3
        %span
          = translate("favourites_list.title")
    .content-form
      - if current_user.blank?
        %h4= translate("favourites.need_sign_in")
      - else
        = render "cart_favourites/list_of_jewels"          